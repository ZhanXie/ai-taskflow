services:
  postgres:
    image: postgres:16
    container_name: ai-taskflow-postgres
    environment:
      POSTGRES_DB: ai_taskflow
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d ai_taskflow"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data: